<%= form_with(model: faceshape, local: true) do |form| %>
  <% if faceshape.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(faceshape.errors.count, "error") %> prohibited this faceshape from being saved:</h2>

      <ul>
        <% faceshape.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
