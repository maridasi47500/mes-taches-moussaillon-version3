<%= form_with(model: barcriterion, local: true) do |form| %>
  <% if barcriterion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(barcriterion.errors.count, "error") %> prohibited this barcriterion from being saved:</h2>

      <ul>
        <% barcriterion.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>

  <% end %>
<%=image_tag "/assets/"+barcriterion.bar.image%>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.hidden_field :gooduser_id, value: current_user.id %>
  </div>

  <div class="field">
    <%= form.hidden_field :bar_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
