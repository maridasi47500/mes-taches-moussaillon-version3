<%= form_with(model: plantspecie, local: true) do |form| %>
  <% if plantspecie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(plantspecie.errors.count, "error") %> prohibited this plant specie from being saved:</h2>

      <ul>
        <% plantspecie.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= render "form/imagefield", form: form, myfield: "image", apercu: "apercu" %>
  </div>

  <div class="actions">
    <%= form.submit "créer une espèce naturelle de plante" %>
  </div>
<% end %>
