<%= form_with(model: eyecolor, local: true) do |form| %>
  <% if eyecolor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(eyecolor.errors.count, "error") %> prohibited this eyecolor from being saved:</h2>

      <ul>
        <% eyecolor.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
