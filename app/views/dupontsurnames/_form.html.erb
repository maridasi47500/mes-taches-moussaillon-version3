<%= form_with(model: dupontsurname, local: true) do |form| %>
  <% if dupontsurname.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dupontsurname.errors.count, "error") %> prohibited this dupontsurname from being saved:</h2>

      <ul>
        <% dupontsurname.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
