<%= form_with(model: synonym, local: true) do |form| %>
  <% if synonym.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(synonym.errors.count, "error") %> prohibited this synonym from being saved:</h2>

      <ul>
        <% synonym.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :synonym_id %>
    <%= form.number_field :synonym_id %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
