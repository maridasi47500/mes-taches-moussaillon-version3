<%= form_with(model: clothescolor, local: true) do |form| %>
  <% if clothescolor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(clothescolor.errors.count, "error") %> prohibited this clothescolor from being saved:</h2>

      <ul>
        <% clothescolor.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
