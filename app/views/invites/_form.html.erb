<%= form_with(model: invite, local: true) do |form| %>
  <% if invite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(invite.errors.count, "error") %> prohibited this invite from being saved:</h2>

      <ul>
        <% invite.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :namegroup, "name for group with members invited" %>
    <%= form.select :namegroup, [['Jeune Marié(e)','YM'],['Amis','AM'],['Famille','FA'],['Acteur','AC']], {include_blank: true} %>
  </div>

  <div class="field">
    <%= form.label :vipassname, "short name for vip pass" %>
    <%= form.text_field :vipassname%>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= render "form/imagefield", form: form, myfield: "image", apercu: "apercu" %>
  </div>
  <div class="field">
<%=render "form/checkbox", form: form, fieldname: 'person_ids', collec: invite.people, mesmodele:"people"%>
</div>

  <div class="field">
<%=render "form/checkbox", form: form, fieldname: 'task_ids', collec: invite.tasks, mesmodele:"tasks"%>
</div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
<%=content_for :myjscode do%>
<script>
<%@search=[json_tasks_path,personjson_albums_path]%>
<%@select=[tasks_invites_path,people_invites_path]%>
<%@nom=['tasks','people']%>
<%=render partial:"form/functionjson", collection: @nom.zip(@search, @select), as: :t%>
</script>
<%end%>

