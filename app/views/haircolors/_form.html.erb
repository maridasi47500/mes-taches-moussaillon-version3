<%= form_with(model: haircolor, local: true) do |form| %>
  <% if haircolor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(haircolor.errors.count, "error") %> prohibited this haircolor from being saved:</h2>

      <ul>
        <% haircolor.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
