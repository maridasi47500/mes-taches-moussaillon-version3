<%= form_with(model: admissionfile, local: true) do |form| %>
  <% if admissionfile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admissionfile.errors.count, "error") %> prohibited this admissionfile from being saved:</h2>

      <ul>
        <% admissionfile.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
