<%= form_with(model: m17nphrase, local: true) do |form| %>
  <% if m17nphrase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(m17nphrase.errors.count, "error") %> prohibited this m17nphrase from being saved:</h2>

      <ul>
        <% m17nphrase.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <% M17nphrase.locale_columns(:content).each do |column| %>

  <div class="field">
    <%= form.label column %>
    <%= form.text_area column %>
  </div>
<%end%>
  <div class="field">
<%=render "form/checkbox", form: form, fieldname: 'person_ids', collec: Person.findbyid(i18nphrase.person_ids), mesmodele:"people"%>

</div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%=content_for :myjscode do%>
<script>
<%@select=[people_m17nphrases_path]%>
<%@search=[personjson_albums_path]%>
<%@nom=['people']%>
<%=render partial:"form/functionjson", collection: @nom.zip(@search, @select), as: :t%>
</script>
<%end%>

