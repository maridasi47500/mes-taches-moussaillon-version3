<%= form_with(model: bacseries, local: true) do |form| %>
  <% if bacseries.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bacseries.errors.count, "error") %> prohibited this bacseries from being saved:</h2>

      <ul>
        <% bacseries.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
