<%= form_with(model: google_request, local: true) do |form| %>
  <% if google_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(google_request.errors.count, "error") %> prohibited this google_request from being saved:</h2>

      <ul>
        <% google_request.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
