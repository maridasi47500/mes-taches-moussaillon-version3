<%= form_with(model: moviescene, local: true) do |form| %>
  <% if moviescene.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(moviescene.errors.count, "error") %> prohibited this moviescene from being saved:</h2>

      <ul>
        <% moviescene.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
