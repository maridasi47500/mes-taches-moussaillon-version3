<%= form_with(model: historical_character, local: true) do |form| %>
  <% if historical_character.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(historical_character.errors.count, "error") %> prohibited this historical_character from being saved:</h2>

      <ul>
        <% historical_character.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= render "form/imagefield", form: form, myfield: "image", apercu: "apercu" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
