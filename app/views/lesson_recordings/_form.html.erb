<%= form_with(model: lesson_recording, local: true) do |form| %>
  <% if lesson_recording.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lesson_recording.errors.count, "error") %> prohibited this lesson_recording from being saved:</h2>

      <ul>
        <% lesson_recording.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
