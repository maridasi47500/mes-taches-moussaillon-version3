<p id="notice"><%= notice %></p>

<h1>Robots</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Image</th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <% @robots.each do |robot| %>
      <tr>
        <td><%= robot.name %></td>
        <td><%= robot.description %></td>
        <td><%= image_tag robot.image, class: "imageprofilethread" %></td>
        <td><%= link_to "poster à propos de ce robot", "/posts/new?robotid=#{robot.id}" %></td>
        <td><%= link_to 'Show', robot %></td>
        <td><%= link_to 'Edit', edit_robot_path(robot) %></td>
        <td><%= link_to 'Destroy', robot, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Robot', new_robot_path %>
