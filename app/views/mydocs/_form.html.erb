<%= form_with(model: mydoc, local: true) do |form| %>
  <% if mydoc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mydoc.errors.count, "error") %> prohibited this mydoc from being saved:</h2>

      <ul>
        <% mydoc.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.hidden_field :controller %>
  </div>

  <div class="field">
    <%= form.label :action %>
    <%= form.hidden_field :action %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content,style:"width:100%;min-height:300px;" %>
  </div>

  <div class="field">
    <%= form.hidden_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
