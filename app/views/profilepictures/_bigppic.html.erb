 <div class="containersubtitle">
  <%= profilepicture.nameimage%><%= profilepicture.name && profilepicture.name.length > 0 ? (image_tag(asset_path(profilepicture.name), class: "bigppic", alt:" pic")) : ""%>
  <div id="mypersonsubtitle" class="subtitle"></div>
</div>

