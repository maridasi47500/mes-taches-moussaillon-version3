<%= form_with(model: musicalbum, local: true) do |form| %>
  <% if musicalbum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(musicalbum.errors.count, "error") %> prohibited this musicalbum from being saved:</h2>

      <ul>
        <% musicalbum.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= render "form/imagefield", form: form, myfield: "image", apercu: "apercu" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
