<%= form_with(model: peoplerelation, local: true) do |form| %>
  <% if peoplerelation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(peoplerelation.errors.count, "error") %> prohibited this peoplerelation from being saved:</h2>

      <ul>
        <% peoplerelation.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
