<%= form_with(model: aregoodrequest, local: true) do |form| %>
  <% if aregoodrequest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(aregoodrequest.errors.count, "error") %> prohibited this aregoodrequest from being saved:</h2>

      <ul>
        <% aregoodrequest.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
