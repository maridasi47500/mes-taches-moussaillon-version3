<%= form_with(model: hardwareitem, local: true) do |form| %>
  <% if hardwareitem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hardwareitem.errors.count, "error") %> prohibited this hardwareitem from being saved:</h2>

      <ul>
        <% hardwareitem.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
