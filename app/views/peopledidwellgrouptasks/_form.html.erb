<%= form_with(model: peopledidwellgrouptask, local: true) do |form| %>
  <% if peopledidwellgrouptask.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(peopledidwellgrouptask.errors.count, "error") %> prohibited this peopledidwellgrouptask from being saved:</h2>

      <ul>
        <% peopledidwellgrouptask.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


    <%= form.hidden_field :sailorgrouptask_id %>
<%(1..5).to_a.each do |g|%>
<input id="imageattitude<%=g%>" type="image" width="50" height="50" alt="attitude" name="task<%=g%>"
       src="/assets/starrum.png">
<%end%>

    <%= form.hidden_field :user_id %>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
