<%= form_with(model: onetime, local: true) do |form| %>
  <% if onetime.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(onetime.errors.count, "error") %> prohibited this onetime from being saved:</h2>

      <ul>
        <% onetime.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :pronoun_id %>
    <%= form.number_field :pronoun_id %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
