<%= form_with(model: tvseries, local: true) do |form| %>
  <% if tvseries.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tvseries.errors.count, "error") %> prohibited this tvseries from being saved:</h2>

      <ul>
        <% tvseries.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<% Tvseries.locale_columns(:name).each do |column| %>
  <p>
    <%= form.label column %>
    <%= form.text_field column %>
  </p>
<% end %>


  <div class="field">
    <%= form.label :image %>
    <%= render "form/imagefield", form: form, myfield: "image", apercu: "apercu",multiple:false %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>
<div class="mypeople">
      <%= form.label :actor_ids %>
      <%= form.collection_check_boxes(:actor_ids, tvseries.actors, :id, :name) do |ff| %>
        <%=ff.check_box%>
<%=render Person.find(ff.value)%>
                            <%end%>
      </div>
      <div><%= text_field_tag :cherchonsattici, "",onchange:"chercheractors(this,cherchewtla);return false;", id: "cherchonswtici",class:"mysearchfieldavatars" %></div>
        <select onchange="selectactors(this,attwt);return false;" id="cherchewtla" data-field="action_ids"></select>
<div id="attwt"></div>
</div>
  <div class="field">
<%=render "form/checkbox", form: form, fieldname: "place_ids", collec: tvseries.places, mesmodele:"places"%>
</div>



  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
<%=content_for :myjscode do%>
<script>
<%@select=[places_tvseries_index_path]%>
<%@search=[json_places_path]%>
<%@nom=['places']%>
<%=render partial:"form/functionjson", collection: @nom.zip(@search, @select), as: :t%>
</script>
<%end%>

<%=content_for :myjscode do%>
<script>

function chercheractors(field,al){
$.ajax({url: '<%=personjson_people_path%>?name='+field.value, 
success:function(data){
al.innerHTML="<option></option>";
var value, id;
for (var i=0;i<data.length;i++){
id=data[i].id;
name=data[i].name;
al.innerHTML+="<option value='"+id+"'>"+name+"</option>";
}
}});
};
function selectactors(field,greatpeople){
$.ajax({url: '<%=actors_tvseries_index_path%>?id='+field.value, 
success:function(data){
greatpeople.innerHTML+=data;
}});
};
</script>
<%end%>

