<%= form_with(model: computingstuff, local: true) do |form| %>
  <% if computingstuff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(computingstuff.errors.count, "error") %> prohibited this computingstuff from being saved:</h2>

      <ul>
        <% computingstuff.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= render "form/imagefield", form: form, myfield: "image", apercu: "apercu" %>
  </div>
  <div class="field">
<%=render "form/checkbox", form: form, fieldname: "person_ids", collec: computingstuff.people, mesmodele:"people"%>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
<%=content_for :myjscode do%>
<script>
<%@search=[personjson_albums_path]%>
<%@select=[people_computingstuffs_path]%>

<%@nom=['people']%>
<%=render partial:"form/functionjson", collection: @nom.zip(@search, @select), as: :t%>
</script>
<%end%>

