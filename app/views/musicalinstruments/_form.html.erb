<%= form_with(model: musicalinstrument, local: true) do |form| %>
  <% if musicalinstrument.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(musicalinstrument.errors.count, "error") %> prohibited this musical instrument from being saved:</h2>

      <ul>
        <% musicalinstrument.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <% [:name_en, :name_fr,:name_de].each do |column| %>

  <div class="field">
    <%= form.label column %>
    <%= form.text_field column %>
  </div>
<%end%>

  <div class="field">
    <%= form.label :image %>
    <%= render "form/imagefield", form: form, myfield: "image", apercu: "apercu" %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>
  <div class="field">
<%=render "form/checkbox", form: form, fieldname: "player_ids", collec: musicalinstrument.players, mesmodele:"players"%>
</div>


  <div class="actions">
    <%= form.submit "Edit your musical instrument" %>
  </div>
<% end %>
<%=content_for :myjscode do%>
<script>
<%@search=[personjson_albums_path]%>
<%@select=[players_musicalinstruments_path]%>
<%@nom=['players']%>
<%=render partial:"form/functionjson", collection: @nom.zip(@search, @select), as: :t%>
</script>
<%end%>

