<%= form_with(model: shaktistatement, local: true) do |form| %>
  <% if shaktistatement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shaktistatement.errors.count, "error") %> prohibited this shaktistatement from being saved:</h2>

      <ul>
        <% shaktistatement.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content_fr %>
    <%= form.text_area :content_fr %>
  </div>

  <div class="field">
    <%= form.label :content_en %>
    <%= form.text_area :content_en %>
  </div>

  <div class="field">
    <%= form.label :content_de %>
    <%= form.text_area :content_de %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
