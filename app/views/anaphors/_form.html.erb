<%= form_with(model: anaphor, local: true) do |form| %>
  <% if anaphor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(anaphor.errors.count, "error") %> prohibited this anaphor from being saved:</h2>

      <ul>
        <% anaphor.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content_fr %>
    <%= form.text_area :content_fr %>
  </div>

  <div class="field">
    <%= form.label :content_en %>
    <%= form.text_area :content_en %>
  </div>

  <div class="field">
    <%= form.label :content_de %>
    <%= form.text_area :content_de %>
  </div>

  <div class="field">
    <%= form.label :epoch_id %>
    <%= form.number_field :epoch_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
