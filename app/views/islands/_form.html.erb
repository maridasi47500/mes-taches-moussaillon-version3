<%= form_with(model: island, local: true) do |form| %>
  <% if island.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(island.errors.count, "error") %> prohibited this island from being saved:</h2>

      <ul>
        <% island.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :address_id %>
    <%= form.number_field :address_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  <div class="field">
    <%= form.label :beach_ids,"select beaches" %>
  <%=form.select("beach_ids", Beach.all.collect {|p| [ p.name, p.id ] }, { include_blank: true,selected: island.beach_ids },{multiple:true,  size: 10})%>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
