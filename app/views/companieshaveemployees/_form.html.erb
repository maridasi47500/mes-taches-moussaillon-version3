<%= form_with(model: companieshaveemployee, local: true) do |form| %>
  <% if companieshaveemployee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(companieshaveemployee.errors.count, "error") %> prohibited this companieshaveemployee from being saved:</h2>

      <ul>
        <% companieshaveemployee.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
<%=render "form/radiobutton", form: form, fieldname: 'employee_id', collec: Person.findbyid(companieshaveemployee.employee_id), mesmodele:"employees"%>

  </div>

  <div class="field">
<%=render "form/radiobutton", form: form, fieldname: 'employer_id', collec: Person.findbyid(companieshaveemployee.employer_id), mesmodele:"employers"%>

  </div>

  <div class="field">
<%=render "form/radiobutton", form: form, fieldname: 'job_id', collec: Job.findbyid(companieshaveemployee.job_id), mesmodele:"jobs"%>

  </div>

  <div class="field">
<%=render "form/radiobutton", form: form, fieldname: 'company_id', collec: Company.findbyid(companieshaveemployee.company_id), mesmodele:"companies"%>
  </div>
  <div class="field">
<%=render "form/radiobutton", form: form, fieldname: 'nationality_id', collec: Nationality.findbyid(companieshaveemployee.nationality_id), mesmodele:"nationalities"%>
  </div>

    <%= form.hidden_field :user_id %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
<%=content_for :myjscode do%>
<script>
<%@select=[employee_companieshaveemployees_path,employer_companieshaveemployees_path,job_companieshaveemployees_path,company_companieshaveemployees_path,nationality_companieshaveemployees_path]%>
<%@search=[personjson_albums_path, personjson_albums_path,json_jobs_path,json_companies_path,json_nationalities_path]%>
<%@nom=['employees','employers','jobs','companies','nationalities']%>
<%=render partial:"form/functionjson", collection: @nom.zip(@search, @select), as: :t%>
</script>
<%end%>

