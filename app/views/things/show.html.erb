<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @thing.name %>
</p>

<p>
  <strong>Image:</strong>
  <%= @thing.image ? (image_tag(@thing.image, class: "imageprofilethread", alt:" pic")) : nil%>
</p>

<%= link_to 'Edit', edit_thing_path(@thing) %> |
<%= link_to 'Back', things_path %>
