<%= form_with(model: travelticket, local: true) do |form| %>
  <% if travelticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(travelticket.errors.count, "error") %> prohibited this travelticket from being saved:</h2>

      <ul>
        <% travelticket.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name_fr %>
    <%= form.text_field :name_fr %>
  </div>

  <div class="field">
    <%= form.label :name_en %>
    <%= form.text_field :name_en %>
  </div>

  <div class="field">
    <%= form.label :name_de %>
    <%= form.text_field :name_de %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= render "form/imagefield", form: form, myfield: "image", apercu: "apercu" %>
  </div>
  <div class="field">
<%=render "form/checkboxcolumnlayout",modelname: Alias,l: 'aliases/alias',ln: :a, columnname: 'user_id',form: form, fieldname: 'alias_ids', collec: Alias.findbyid(travelticket.alias_ids), mesmodele:"aliases"%>

</div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
<%=content_for :myjscode do%>
<script>
<%@search=[personjson_albums_path]%>
<%@select=[aliases_traveltickets_path]%>

<%@nom=['aliases']%>
<%=render partial:"form/functionjson", collection: @nom.zip(@search, @select), as: :t%>
</script>
<%end%>

