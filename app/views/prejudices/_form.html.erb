<%= form_with(model: prejudice, local: true) do |form| %>
  <% if prejudice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prejudice.errors.count, "error") %> prohibited this prejudice from being saved:</h2>

      <ul>
        <% prejudice.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content_en %>
    <%= form.text_field :content_en %>
  </div>

  <div class="field">
    <%= form.label :content_fr %>
    <%= form.text_field :content_fr %>
  </div>

  <div class="field">
    <%= form.label :content_de %>
    <%= form.text_field :content_de %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
