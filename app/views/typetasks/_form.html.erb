<%= form_with(model: typetask, local: true) do |form| %>
  <% if typetask.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(typetask.errors.count, "error") %> prohibited this typetask from being saved:</h2>

      <ul>
        <% typetask.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
