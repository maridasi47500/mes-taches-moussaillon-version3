<%= form_with(model: makeupstyle, local: true) do |form| %>
  <% if makeupstyle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(makeupstyle.errors.count, "error") %> prohibited this makeupstyle from being saved:</h2>

      <ul>
        <% makeupstyle.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
