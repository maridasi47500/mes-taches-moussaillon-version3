<%= form_with(model: skin_color, local: true) do |form| %>
  <% if skin_color.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(skin_color.errors.count, "error") %> prohibited this skin_color from being saved:</h2>

      <ul>
        <% skin_color.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
