<p id="notice"><%= notice %></p>

<h1>Songs</h1>
<br>
<%= button_to 'refresh', refreshsongradio_songs_path, method: :post,onclick: "return confirm('êtes vous sûr de vouloir rafraichir les chansons depuis le fil de radio ?');", data: { confirm: 'Are you sure?' } %>
<br>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @songs.each do |song| %>
      <tr>
        <td><%= song.name %></td>
        <td><%= image_tag song.image, class: "imageprofilethread" %></td>
        <td><%= link_to 'Show', song %></td>
        <td><%= link_to 'Edit', edit_song_path(song) %></td>
        <td><%= link_to 'Compose', composer_song_path(song.id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<h1>Songs</h1>


    <div class="row row-cols-1 row-cols-md-5 g-4">


    <% @compositions.each do |song| %>
  <div class="col">
    <div class="card ">
      <%=link_to compo_path(song.mycompoid) do%>
      <p><%= song.title %>
        <%= image_tag "/assets/"+(song.image1||song.image2), class: "imageprofilethread" %>
        <%= (song.composername) %>
        <%= image_tag "/assets/"+(song.composerimage), class: "imageprofilethread" %>

      </p>
    <%end%>
    </div>
    </div>
    <% end %>
    </div>

<br>

<%= link_to 'New Song', new_song_path %>
