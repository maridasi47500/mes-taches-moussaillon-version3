
<%=fields_for :post, @post do |form|%>
<%=form.fields_for :my_posts_surnames do |f|%>

      <%= f.label :destinataire_id, "le message est pour qui-est destiné à qui ?" %>

      <%= f.collection_radio_buttons(:destinataire_id, @mesdestinataires, :id, :name) do |ff| %>

        <%=ff.radio_button(autocomplete:"off",class: "btn-check")%>

        <%=ff.label(class: "btn btn-secondary") do%>
          <%=render Person.find(ff.value)%>
        <%end%>

<button onclick='$.ajax({url: "/persondisplay/<%=ff.value%>",success:function(data){$(".editable_fr").append(data);$("#post_content_fr").val($("#post_content_fr").val()+data); }});' id="btn<%=ff.value%>" type="button">add him/her to content</button>
          <button onclick='$.ajax({url: "/persondisplay/<%=ff.value%>",success:function(data){$(".editable_fr").append(data);$("#post_my_posts_surnames_attributes_0_content_fr").val($("#post_my_posts_surnames_attributes_0_content_fr").val()+data);$("#post_my_posts_surnames_attributes_0_content_fr").focus(); }});' id="btn<%=ff.value%>" type="button">add him/her to content of the posts surname</button>
                            
    <%end%>
                                <%=  f.radio_button( "destinataire_id", nil)%><%= f.label "destinataire_id", "personne"%>

      </div>
    <%end%>
    <%end%>
