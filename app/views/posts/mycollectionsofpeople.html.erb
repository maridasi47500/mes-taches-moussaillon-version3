<%@collections.each do |collection|%>
<%=link_to collection.name, "", onclick: "$.ajax({type:'post',url:'#{mycollectionofpeople_collection_path(collection.id)}',success:function(code){$('.mypeople').html(code.split('<br>')[0]);console.log(code);$('#maprofilpic').html(code.split('<br>')[1]);}});return false;"%>
<%end%>