<%= form_with(model: filmparody, local: true) do |form| %>
  <% if filmparody.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(filmparody.errors.count, "error") %> prohibited this filmparody from being saved:</h2>

      <ul>
        <% filmparody.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name_fr %>
    <%= form.text_field :name_fr %>
  </div>

  <div class="field">
    <%= form.label :name_en %>
    <%= form.text_field :name_en %>
  </div>

  <div class="field">
    <%= form.label :name_de %>
    <%= form.text_field :name_de %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= render "form/imagefield", form: form, myfield: "image", apercu: "apercu" %>
  </div>
<div class="mypeople">
      <%= form.label :actor_ids %>
      <%= form.collection_check_boxes(:actor_ids, filmparody.actors, :id, :name) do |ff| %>
        <%=ff.check_box%>
<%=render Person.find(ff.value)%>
                            <%end%>
      </div>
      <div><%= text_field_tag :cherchonsattici, "",onchange:"chercheractors(this,cherchewtla);return false;", id: "cherchonswtici",class:"mysearchfieldavatars" %></div>
        <select onchange="selectactors(this,attwt);return false;" id="cherchewtla" data-field="action_ids"></select>
<div id="attwt"></div>
</div>
  <div class="field">
<%=render "form/checkbox", form: form, fieldname: "place_ids", collec: filmparody.places, mesmodele:"places"%>
</div>



  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
<%=content_for :myjscode do%>
<script>
<%@select=[places_filmparodies_path]%>
<%@search=[json_places_path]%>
<%@nom=['places']%>
<%=render partial:"form/functionjson", collection: @nom.zip(@search, @select), as: :t%>
</script>
<%end%>

<%=content_for :myjscode do%>
<script>

function chercheractors(field,al){
$.ajax({url: '<%=personjson_people_path%>?name='+field.value, 
success:function(data){
al.innerHTML="<option></option>";
var value, id;
for (var i=0;i<data.length;i++){
id=data[i].id;
name=data[i].name;
al.innerHTML+="<option value='"+id+"'>"+name+"</option>";
}
}});
};
function selectactors(field,greatpeople){
$.ajax({url: '<%=actors_filmparodies_path%>?id='+field.value, 
success:function(data){
greatpeople.innerHTML+=data;
}});
};
</script>
<%end%>

